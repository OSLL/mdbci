[
{
   "product":		"maxscale",
   "version":           "default",
   "repo":		"${production_url}/$version/ubuntu ${platform_version} main ",
   "repo_key":		"${deb_repo_key}",
   "platform":		"ubuntu",
   "platform_version":	"${platform_version}"
}
]
