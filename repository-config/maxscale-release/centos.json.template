[
{
   "product":		"maxscale",
   "version":		"default",
   "repo":		"${production_url}/$version/centos/${platform_version}/\$basearch",
   "repo_key":		"${rpm_repo_key}",
   "platform":		"centos",
   "platform_version":	"${platform_version}"
}
]
