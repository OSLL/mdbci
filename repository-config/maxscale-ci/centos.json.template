[
{
   "product":		"maxscale",
   "version":		"default",
   "repo":		"${ci_url}/$ci/mariadb-maxscale/centos/${platform_version}/\$basearch",
   "repo_key":		"${rpm_repo_key}",
   "platform":		"centos",
   "platform_version":	"${platform_version}"
}
]
