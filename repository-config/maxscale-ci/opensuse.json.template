[
{
   "product":		"maxscale",
   "version":		"default",
   "repo":		"${ci_url}/$ci/mariadb-maxscale/opensuse/${platform_version}/\$basearch",
   "repo_key":		"${rpm_repo_key}",
   "platform":		"opensuse",
   "platform_version":	"${platform_version}"
}
]
